# 壹拳俱乐部 — Design Context（全站统一设计系统）

> 本规范同时约束**公共端（首页 / 挑战详情 / 创建 / 顶栏 / 公共组件）** 与 **管理端（后台登录 / 管理面板）**。二者共用同一套令牌与原语，不再分两套语言。

## Design Context

### Users
- 普通访客：浏览挑战、参与押注、发布小作文。
- 网站运营者 / 管理员：管理挑战、参与者、数据备份与官网配置；桌面办公与手机随时操作，移动端必须真正可用。

### Brand Personality
健身 / 健康挑战平台「壹拳俱乐部」。品牌主色为橙色（温暖、能量、活力）。语调：专业、可信、有冲劲。三词：energetic · trustworthy · focused。

### Aesthetic Direction
深色「运营驾驶舱（Mission Control）」风格。有温度的深炭灰分层 + 发丝级描边 + 克制的品牌橙强调色 + 性格标题字体（Space Grotesk）。Anti-reference：霓虹紫蓝渐变、纯黑纯白、毛玻璃滥用、千篇一律的卡片流、两套并行配色。

### Design Principles
1. **单一真相源**：所有颜色来自 `src/index.css` 的 `var(--x)` 令牌；所有按钮/输入/徽章/卡片/弹窗/分段来自 `src/components/ui/*` 原语。禁止页面内硬编码 `neutral-*` / `orange-*` / 裸 `oklch()`。
2. **圆角四级**：`rounded-lg`(8px) 紧凑控件/Tab；`rounded-xl`(12px) 主/次按钮/输入框；`rounded-2xl`(16px) 卡片/面板/弹窗；`rounded-full` 状态/站队/结果/情绪徽章（统一 pill）、头像、开关。
3. **按钮层级**：`primary`（实心 accent + 阴影 + hover:brightness）/ `secondary`（surface + 描边）/ `ghost`（文字 + hover 底色）/ `success`（绿柔）/ `danger`（红柔）。全站同一组件，禁止各处手写。
4. **语义色映射（同一含义全站同色）**：状态 active→ok-soft(绿柔) / pending→warn-soft(黄柔) / completed→muted(灰)；结果 win→ok / lose→bad；站队/情绪 support/看涨→side-support(蓝) / oppose/看跌→side-oppose(紫)；危险→bad。
5. **字体**：所有 `h1/h2/h3` 自动用 `font-display`（Space Grotesk）；正文 Inter。
6. **焦点与动效**：所有交互元素有 `:focus-visible` accent 焦点环；过渡 `ease-out`，hover 用 `brightness` / `--hover`，不用回弹。
7. **响应式**：关键操作不隐藏；移动端点击目标 ≥ 44px。
