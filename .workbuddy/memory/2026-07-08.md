# 2026-07-08

## 设计系统统一改造（fitness-website）
对健身挑战项目做了全站视觉与交互规范统一（用户明确要求大范围修改）。

### 关键约定（后续改动请遵循）
- 单一令牌来源：`src/index.css` 的 `:root`（深色主题）。颜色全部用 `var(--*)`，禁止散落 `oklch()` 字面量；
  本次补充了 `--info/--purple` 及 soft/line 变体，以及阴影令牌 `--shadow-xs/sm/md/lg/accent`。
- `tailwind.config.js` 已注册 `boxShadow` 令牌（`shadow-accent` 等）。
- 按钮交互规范集中在 `Button`/`IconButton` 的 base 类：focus-visible 焦点环、active 缩放、disabled 降透明度+禁指针。
  变体：primary/secondary/ghost/success/danger/info/purple；尺寸 sm/md/lg 圆角对齐 --r-sm/--r-md。
- 全站交互元素统一走共享组件：`Button`/`IconButton`/`Input`/`Textarea`/`Select`/`Tabs`/`Card`/`Modal`/`Badge`。
  管理页原本约 30 个散落原生 button/input 已全部重构为组件。
- 危险按钮统一为 soft 风格（`bg-[var(--bad-soft)] text-[var(--bad)]`），不再混用实心红。
- 布局：内容容器统一 `max-w-7xl mx-auto` + `px-4 sm:px-6 lg:px-8`。

### 验证
`npm run check` 与 `npm run build` 均通过。Tabs 的 onChange 用箭头 `(v)=>setX(v)` 包裹以避免泛型推断为 string。
IconButton 已扩展支持全部 Button 变体。

## 红涨绿跌（利多红/利空绿）落地
用户确认采用 A 股惯例：涨/利多/盈利=红，跌/利空/亏损=绿。
- 在 `index.css` 新增令牌 `--up`(红)/`--down`(绿) 及 soft/line 变体（色相与既有 `--bad`/`--ok` 一致）。
- 落地场景：`ChallengeDetailPage` 的「奖金结算」区。
  - 挑战者结果：hostSuccess(盈利)→红(--up)，失败(亏损)→绿(--down)。
  - 挑战者付出：hostPayout>0(掏钱/亏损)→绿(--down)，==0(不掏钱/盈利)→红(--up)。
  - 赢家人数/奖池总额改为中性色 --text，避免与"绿=亏"冲突。标题加"（红涨绿跌）"小注。
- 该约定已写入跨项目长期记忆 `~/.workbuddy/MEMORY.md`。
- 注意：通用 UI 的"危险/错误"仍用红（--bad），红涨绿跌仅用于金融方向的盈/亏、涨/跌语义，二者不混。

## 支持/反对两侧改红绿（利多红/利空绿）
用户指出挑战里"支持/反对"（看多/看空 = 利多/利空）两侧原为蓝(--side-support)和紫(--side-oppose)，
要求改为红/绿。已将 `--side-support*` 别名到 `--up*`(红)、`--side-oppose*` 别名到 `--down*`(绿)。
影响面：Badge 两侧徽章、ChallengeDetailPage 看多/看空标签与选边按钮、AdminPanelPage 参与者侧徽章、EssayDetailModal 选边。
现全站"多=红、空=绿"与结算区（hostSuccess/盈利=红）完全统一、单一来源。

## 首页卡片全新设计（现代化）
重做 `src/components/challenge/ChallengeCard.tsx`（用户要求"更符合现代设计规范"）。
- 默认 `--shadow-sm` 轻投影；hover 上浮 `-translate-y-1` + `--shadow-lg` + 强调色描边；封面图 `scale-110` 缓动放大。
- 封面状态/封档/参与人数改为 `backdrop-blur` 玻璃拟态药丸 + 顶部高光；参与人数移入封面左上。
- 进度条改 `h-1.5` 圆角胶囊 + accent→accent-deep 渐变填充，包在 `surface-2` 卡片内。
- 小作文区从全宽出血改为"卡片内卡片"圆角块（role=button + 键盘可达），利多/利空沿用 support/oppose 红绿令牌。
- 发起人改为 `@昵称 发起` 手柄式；次要文字提亮为 `--muted` 提升对比度。
- 全程复用既有令牌，无散落硬编码；HomePage 仅消费 ChallengeCard，无需改动。
- 注意：show_widget 预览系统是浅色扁平规范（禁渐变/阴影/模糊），不适合预览此类深色层次卡片，故未用其出图。

## 首页卡片 + 卡片弹窗二次全新设计（UI Designer 专家）
用户要求"全新界面：现代、美观、易用"，并明确：① 小作文去掉左边图标；② 进行中/待确认/已结束要有对应颜色。

### 状态色（新增 Badge 变体，与金融红涨绿跌分离避免误读）
在 `Badge.tsx` 增加 `status-active / status-pending / status-ended` 三变体：
- 进行中 → 蓝 `--info`（直播/进行感，带 animate-ping 脉冲点）
- 待确认 → 琥珀 `--warn`（待处理/需关注）
- 已结束 → 中性灰 `--muted`（已归档）
卡片封面状态药丸改为"玻璃底 + 状态色圆点"，进行中带 ping 脉冲；卡片内容区不重复显示状态，避免冗余。

### 首页卡片（`ChallengeCard.tsx`）
- 去掉小作文左侧 MessageCircle 圆图标，改为"最新小作文"标签置顶 + 下方 利多/利空徽章 + 摘要。
- 封面 hover 缩放从 scale-110 收敛为 scale-105（更克制）；状态药丸 padding 加大更易读。

### 卡片弹窗（`EssayDetailModal.tsx`）
- 每条小作文左侧加情感色条（利多=红 --up / 利空=绿 --down），与红涨绿跌统一；圆角+细边框更精致。
- 删除按钮改为 hover 才显现（opacity-0 group-hover:opacity-100，focus-visible 仍可见），界面更干净。
- 空状态改为虚线描边容器。
- 发布弹窗的"观点"选择器改为现代分段控件：选中态用情感色 soft 填充 + 文字染情感色（红/绿），未选为 surface-2。
- check + build 均通过。

## 卡片内部间距修复
用户反馈"卡片内之间的间距不对"。根因：进度条块（剩余时间+进度条）只对 active 且未过期挑战渲染，其余状态不渲染；而小作文块用 `mt-auto` 顶到底 → 不同状态卡片"日期→小作文"间距忽大忽小。
修复：
- `ChallengeCard` 小作文块 `mt-auto` → `mt-4`，使"日期→下一块"间距在任何状态一致。
- 卡片 `<Link>` 加 `h-full`，`HomePage` 两个网格的 wrapper div 加 `h-full` → 同行卡片等高、底边对齐（多余空间落到底部当留白，而非浮在中间成怪缝）。
- check + build 均通过。

## 状态色调整 + 弹窗一致性重构（用户三点反馈）
1. **进行中改为绿色**：用户指出"进行中应该是绿色"。原 `status-active` 用蓝(--info)；现统一为绿(--ok)，与后台 AdminPanelPage 一直用的 `success`(绿) 一致。
   - 新建 `src/components/ui/StatusPill.tsx`：`StatusPill` + `getEffectiveStatus`（active 过期→pending 逻辑集中，卡片/弹窗共用，防漂移）。
     进行中=绿(--ok)带 ping 脉冲点、待确认=琥珀(--warn)、已结束=中性灰(--muted)。
   - `Badge.status-active` 同步改为绿（虽然当前未直接使用，保持令牌一致）。
   - 三处状态呈现（卡片封面、弹窗英雄区、后台表格）现已统一为 进行中=绿。
2. **缩略图底部渐变**：`ChallengeCard` 封面底部加 `h-14 bg-gradient-to-t from-[var(--surface)] to-transparent`，让图片底边平滑融入卡片本体（不再硬切）。
3. **卡片弹窗视觉与外面一致（大胆重构）**：`EssayDetailModal` 改用 `hideHeader` + 通栏英雄封面（与首页卡片同一套：封面图 + 渐变遮罩 + 顶部高光 + 玻璃药丸[参与人数/封档/StatusPill] + 标题覆盖层 + 底部融入渐变），下方为 元信息(日期/剩余进度) + "全部小作文 N" 区块(右侧"添加"按钮) + 小作文卡片列表。
   - 状态标签现与卡片同源(StatusPill)，消除"弹窗里进行中不一致"的问题。
   - 封面/状态/渐变/间距/排版与首页卡片成同一设计语言。
- check + build 均通过（1667 模块，CSS 34.41kB）。

## 两个弹窗的 UI 优化
用户确认"挑战详情弹窗"= 挑战详情整页（/challenge/:id，原是路由页），"小作文弹窗"= EssayDetailModal（两个不同组件不要混淆）。

### 1. 挑战详情整页（`ChallengeDetailPage.tsx`）通栏重做
- 改为与首页卡片 / 小作文弹窗同源的**英雄封面**：渐变遮罩 + 顶部高光 + 底部融入渐变 + 玻璃药丸（参与人数 / `StatusPill` 状态[进行中=绿] / 封档）+ 标题覆盖层（主题 + "由 @host 发起"）。
- 容器加 `shadow-[var(--shadow-md)]`；内容区统一 `space-y-6 p-5 sm:p-6`，分区标题统一 `text-base font-bold`；资金池统计 / 奖金结算(红涨绿跌) 改 `rounded-xl bg-[var(--surface-2)] p-4/p-3` 卡片；参与者列表独立分区带 `border-t`。
- 逻辑层（参与 / 结算计算 / 管理员确认）完全不动；引入 `StatusPill`+`getEffectiveStatus` 替代原 `getStatusText`/`statusBadgeVariant`；参与弹窗(Modal)保留并轻微 polish。
- 返回按钮改为 ghost（与卡片一致）。

### 2. 小作文弹窗（`EssayDetailModal.tsx`）精简
- 去掉英雄封面与挑战者/挑战信息（参与人数、状态、日期、剩余进度、标题覆盖层），仅保留**小作文列表 + 添加按钮**。
- 改用共享 `Modal` 头部（title="小作文" + 右上"添加"按钮），删除 hideHeader 与 FALLBACK_COVER/StatusPill 等不再需要的依赖。
- 保留：空状态、每条小作文左侧情感色条(红=利多/绿=利空)、hover 删除、配图、利多/利空徽章 + 时间；AddEssayModal 发布弹窗逻辑不变。
- check + build 均通过（1667 模块，CSS 34.05kB）。

## 挑战详情整页内容区全新层级重做
用户两点反馈：① 封面左上角"多少人参加"药丸去掉；② 标题以下的内容按语义大胆重做层级/排版。

### 改动（ChallengeDetailPage.tsx）
- 移除封面左上角 `Users` 玻璃药丸；保留右上 封档 + StatusPill、底部标题 + "由 @host 发起"。
- 内容区 `space-y-6` → `space-y-7`，重新组织语义分区（统一小节标题样式：左侧色条 `h-4 w-1 rounded-full` + `text-base font-bold`）：
  1. **导语**：goal 提亮为 `--text`(原 muted)，下方 meta 行(起止日期 + 参与人数)；剩余时间/待确认提示卡保留。
  2. **多空对决（新增核心可视化）**：红色=利多方(--up) / 绿色=利空方(--down) 的堆叠比例条(按押注额占比，带 transition 动画) + 两端百分比 + 两张情感色卡片(押注额大数字 + 人数)。无押注时显示虚线空状态。
  3. **奖池与保障**：总奖池(参与方押注合计) / 最高赔付 / 最低赔付 三格。
  4. **奖金结算**：保留红涨绿跌四格。
  5. **参与者列表**：小节标题也改用左侧色条体系，列表行不变。
- 新增计算：`totalStakes / hasStakes / supportPct / opposePct`，全部复用 `--up*`/`--down*` 令牌，无散落硬编码。
- check + build 均通过（1667 模块，CSS 34.39kB）。

## 挑战详情整页标题对齐修复
用户要求标题与"由 @host 发起"和下方内容同一层级、左对齐。
- 移除英雄封面底部绝对定位的标题覆盖层（原 `absolute inset-x-0 bottom-0`，白色 drop-shadow 文本）。
- 将 h1 标题 + "由 @host 发起" 移入内容区最顶部，作为首个子块，与正文共用 `p-5 sm:p-6` 左内边距 → 标题左边缘与下方内容完全对齐。封面图仅保留渐变遮罩 + 顶部高光 + 右上 封档/StatusPill。
- 标题配色改为 `--text`（在 surface 背景上），"由 @host 发起" 用 `--accent`（与卡片 @昵称 / 进度条渐变同源橙）。
- check + build 均通过（CSS 34.11kB）。

## 发起人橙色修正（色相一致性）
用户指出"橙色和卡片的橙色不一样"。
根因：项目里两种暖色色相不同——卡片主橙是 `--accent`(`oklch(0.72 0.16 45)` 偏橙，用于进度条渐变 / 标题hover / 卡片 @昵称)，而上一步发起人那行误用了 `--warn`(`oklch(0.82 0.14 80)` 偏琥珀黄)。
修复：详情页"由 @host 发起"那行 `text-[var(--warn)]` → `text-[var(--accent)]`，与卡片橙色彻底统一。
- check + build 均通过（CSS 34.11kB）。

## 挑战详情整页顺序/文案/规则调整
用户三点要求：
1. 顺序：把"奖池与保障"模块上移到"多空对决"之前（导语→奖池与保障→多空对决→奖金结算→参与者列表）。
2. 移除"总奖池"概念：删去奖池与保障模块的"总奖池"格；"最高赔付/最低赔付"文案明确标注为"挑战的最高赔付/挑战的最低赔付"（grid 改 2 列）。结算区的"奖池总额"是结算结果数据，保留（已向用户说明，可据反馈再决定是否移除）。
3. 规则说明：在"奖池与保障"模块底部加"挑战者规则"信息卡（Info 图标 + info 软底），原样放置用户给的赔付规则文案。"白粉/黑粉"与页面阵营标签 `${hostChar}白/${hostChar}黑` 语义一致（白=支持方/host方，黑=反对方），无需改标签。
- 新增 Info 图标导入（lucide-react）；`--info-soft`/`--info-line` 令牌已存在（index.css:135-136）。
- 后用户更新规则文案（更清晰：挑战成功保底、挑战失败、挑战者补差 ≤≥ 两分支），按新稿替换并把卡片内文案改为分点排版（加粗小标题 + 补差缩进两分支）更易读。
- check + build 均通过（CSS 34.08kB，2026-07-09 凌晨收尾）。
