# 2026-07-09 工作记录

## 品牌改名：健康星球 → 野兽俱乐部
- 全站品牌改名，覆盖所有源码位置（已 grep 确认 src/ 内零遗漏）：
  - index.html `<title>`
  - Header.tsx（logo alt + 站名文字）
  - AdminPanelPage.tsx ×2（侧栏标题 + 移动端标题）
  - AdminLoginPage.tsx ×3（控制台标题 ×2 + 版权）
  - capacitor.config.ts `appName`
  - android strings.xml `app_name` + `title_activity_main`
  - src/data/placeholderImages.ts（封面色 SVG 水印文字）
  - README.md 标题
- dist/ 为构建产物，未手动改（重新 build 即更新）。
- check + build 均通过。

## 统一管理后台「参与管理」页视觉规范
- 用户质疑：参与管理页视觉规范是否与其他管理页不一致。
- 核查结论：颜色主题（--accent）与组件库（自研 components/ui）一致；真正不一致为 ① 标题缺 `font-display`、② 根节点多余 `p-6` 与外层 main 双重内边距。注意：参与管理页本身已渲染在 `max-w-7xl` 容器内，之前探查 Agent 称"未套容器"有误。
- 修改文件：src/pages/ParticipantManagementPage.tsx
  - 根 div 去 `p-6`（对齐其他 Tab 仅用 main padding）。
  - 标题 h2 加 `font-display`，字形与其他管理页统一。
- 保留原生 `<table>`：外层已用 `var(--surface)/rounded-xl/border var(--line)` 卡片式容器，视觉与 Card 一致；且需拖拽排序，表格更合适，未强改 Card 列表。

## 小作文弹窗加"谁的小作文"来源说明
- EssayDetailModal 弹窗 body 顶部新增一行：`来自 @hostName 发起的挑战 · 小作文`（hostName 用 accent 橙、其余 faint 灰），复用 challenge 入参，无需新增 prop。
- 此前用户要求去掉挑战者/挑战信息部分，仅留小作文+添加；本次补回轻量来源标识以明确归属。

## 网站 Logo / Favicon（前序）
- 用户提供的 `健身Logo.png` 复制到 public/logo.png；Header 用 `<img>` 替换原 Dumbbell 图标；index.html favicon 由 favicon.svg 改为 logo.png（image/png）。

## 首页及后台 UI 细节迭代（本轮）
- 封面占位图（`placeholderImages.ts` 的 SVG）去掉烤入的"野兽俱乐部"水印文字，卡片/详情页同源生效。
- 首页移动端下拉菜单：按钮加 `fullWidth` + 容器 `flex flex-col`，改为可靠竖向排列。
- 管理页导航顺序统一为"备份在下、设置在下下方"：桌面侧边栏（挑战→备份→设置）、移动端底部 Tab 栏（挑战→备份→设置，原顺序为 挑战→设置→备份）。顶部下拉菜单不含 tab 切换（与底部栏不重复）。
- 小作文弹窗（`EssayDetailModal`）：去掉"来自 @xx 发起的挑战 · 小作文"来源行；弹窗标题由"小作文"改为"`${hostName}的小作文`"。
- 管理页（`AdminPanelPage` 侧栏+移动顶栏）与登录页（`AdminLoginPage` 桌面左栏+移动品牌区）的渐变圆+Shield/Dumbbell 图标 logo 全部替换为 `/logo.png`；同步移除未使用的 Shield/Dumbbell 导入。
- 首页主标题保留用户指定的"群健康挑战赛"图片（`public/title.png`），未改动；一度误改为"野兽俱乐部"文字后已回滚。仅 `index.html` `<title>` 由"野兽俱乐部 - 群健康挑战赛"简化为"野兽俱乐部"（用户所指"网站名"）。
- 上述改动均 `npm run build` 通过。

## 备份恢复页信息架构重做
- 用户反馈原「备份恢复」页层级不合理：① 页面 H2 与内层 Card 标题"数据备份与恢复"重复；② 按"本地/服务器"分组把「备份/恢复」拆成 2×2 矩阵，扫描成本高；③ 服务器备份列表藏在 Modal 里，自动备份说明重复出现。
- 重做（`AdminPanelPage.tsx`）：删冗余中间 Card 标题，改为两个并列小节「本地备份」「服务器备份」，每节自包含；服务器备份列表内联展示（含 loading/空态），删除原 Modal 与 `showServerBackups` 状态、`Modal` 导入；恢复动作（导入/恢复备份）统一标红 danger。
- 新增 effect：进入 backup tab 时自动 `loadServerBackups()`（依赖 activeTab、isAdminAuthenticated）。
- 类型检查 `tsc -b --noEmit` 通过（exit 0）。

## 创建挑战路由守卫（未登录直跳登录页）
- `CreateChallengePage` 原未登录分支显示"需要管理员权限"中间提示页（带登录按钮）；改为未登录直接 `<Navigate to="/admin-login" replace />`，省掉中间页，点"创建挑战"未登录即直达登录页。
- 补 `react-router-dom` 的 `Navigate` 导入；`Shield`/`Button` 在创建页主体仍使用，无需改动导入。
- 注：登录成功（`AdminLoginPage`）当前固定跳 `/admin`，未做登录后回跳 `/create`。如需"登录后回到创建页"可后续加 redirect 参数。
- `npm run build` 通过。

## Git 推送认证坑（重要，已踩）
- 仓库 `x1ngodlike/fitness-website`（owner=x1ngodlike，个人 User，无组织 SSO）。
- **fine-grained 令牌（`github_pat_` 前缀）不能用于 Git over HTTPS 推送**，只能走 API（Bearer）。现象：API `GET /repos/...` 返回 `permissions.push=True`，但 `git push`（Basic 或 extraHeader Bearer）一律失败——Basic 返回 403 "Permission denied"，Bearer 因本机 GCM askpass 无 TTY 卡在 `could not read Username`。
- **解决办法**：推送必须用 **Classic 经典令牌（`ghp_` 前缀）+ `repo` 范围**，走 Basic 认证（URL 内联 `https://user:ghp_xxx@github.com/...` 或 `GIT_TERMINAL_PROMPT=0`）。或让用户本机 `git push`（GCM 弹浏览器登录）。
- 本机 git 身份缺失：已 `git config user.name "x1n" / user.email "593587956@qq.com"`（沿用历史提交身份）。
- 注意 `.gitignore` 的 `data/` 规则会误伤 `src/data/`，导致已追踪的 `src/data/mockData.ts` 无法普通 `git add`，需 `git add -f`（仅针对已追踪文件，安全）。

## Docker 构建失败根因（UNRAID 部署，已踩 + 已修）
- 症状：`docker build` 在 `npx vite build` 阶段报 `Could not resolve "../data/placeholderImages" from "src/pages/CreateChallengePage.tsx"`（rollup `handleInvalidResolvedId`）。
- 双重根因：
  1. `.dockerignore` 里裸 `data/` 会匹配**任意层级** `data/`，把 `src/data/` 整个排除出构建上下文 → 容器内 `src/data/*` 全部缺失。
  2. `.gitignore` 里同为裸 `data/`，导致 `src/data/placeholderImages.ts` **从未被 git 追踪**（连 untracked 都不显示），git clone 部署时该文件天然缺失。
- 本地 Windows 能 build 过，是因为文件在磁盘上存在、且 Windows 大小写/忽略不敏感；容器是 Linux，.dockerignore 生效即炸。
- 修复（commit `66b493a`）：`.gitignore` 与 `.dockerignore` 的 `data/` 都改为根锚定 `/data/`（仅排除项目根 data/，不碰 src/data/）；并把被忽略的 `placeholderImages.ts` 补 `git add` 提交进仓库。
- 部署同步要点：UNRAID 上若 `.dockerignore` 仍是旧 `data/`，或 clone 的仓库缺 `placeholderImages.ts`，构建仍会挂。端到端正确做法 = 把 `66b493a` 推到 GitHub → UNRAID `git pull` → `./deploy/build.sh`。临时绕过 = UNRAID 上直接删 `.dockerignore` 的 `data/` 行并确保 `src/data/placeholderImages.ts` 存在后重建。
- 【修正】经 `git fetch` + 比对：远程 `origin/main` 当前已 = 本地 `main`（均 `f284e5c8924a221bfef521914c5598de438be5a9`），工作区干净。之前"未推送"判断是误判——实际代码早已成功推上 GitHub（推测用户本机 GCM 推送/合并成功）。故"删除库所有文件重新 force push"**没必要**：远程已是最新全量代码（品牌升级 + ignore 修复 + placeholderImages.ts 均在 f284e5c 内）。
- UNRAID 部署直接 `git pull && ./deploy/build.sh` 即可拿到 `.dockerignore` 修复、重建通过；无需重推。
- 远程另有分支 `x1ngodlike-cuddly-fortnight`、`x1ngodlike-effective-dollop` 及 PR#1（head=1f30e782），force push main 不会删它们，但会重写 main 历史、无收益且有风险。

## UI 微调（2026-07-09 凌晨）
- 首页卡片（`ChallengeCard.tsx`）新增 hover 细线闪烁：`group-hover` 时整卡边缘出现 accent 橙红 1px 细线 + 发光（`shadow var(--accent-shadow)`），由 `index.css` 的 `@keyframes card-line-flicker`（opacity 0.35↔1，1.1s）驱动；保留原上浮 + 封面 scale 动效。
- 详情页头图与首页卡片封面确认均已为 `aspect-[16/9]`（先前通栏重做时定下），"改为 16:9"需求已满足、无需改动。若用户看到的仍非 16:9，多为浏览器缓存/未重新构建所致。
- 注：上述两文件改动尚未 commit/push（相对已同步的 f284e5c 为新增本地改动）。
- 修正（同日凌晨）：详情页头图 `aspect-[16/9]` → `aspect-[21/9]`（用户明确要 21:9 电影画幅）；首页卡片细线闪烁改用纯 CSS（`.card-line` + `.group:hover .card-line` 触发 `@keyframes card-line-flicker`），替代原先 `group-hover:animate-[arbitrary]`——后者在 Tailwind JIT 下生成的 animation 声明不稳定，导致闪烁异常/不生效（即用户反馈的 bug）。
- 挑战详情页顶部改为浮动置顶栏（`ChallengeDetailPage.tsx`）：新增 `sticky top-16 z-40` 顶部栏（紧贴固定的全局 Header `h-16` 下方），含 返回(`navigate(-1)`) / 小作文(打开 `EssayDetailModal`) / 标题(截断居中) / 关闭(`navigate('/')` 回首页) 四元素；用 `bg-[var(--bg-90)] backdrop-blur-xl` 与全局头部 frosted 风格一致；删除原内联在内容流的"返回"按钮；内容容器 `pt-20`→`pt-16`（与栏初始位置对齐、滚动时无缝贴顶）；底部接上 `<EssayDetailModal challenge onClose>`。已 `npm run build` 通过。
- 【修正·同次】首次实现时把 `<EssayDetailModal>` 无条件渲染，导致进详情页就自动弹出小作文（该组件内部 `<Modal open>` 一旦挂载即显示，无 open 受控 prop）。改为 `{showEssay && <EssayDetailModal .../>}` 条件渲染（与 HomePage 一致），仅点顶部"小作文"按钮才打开。已 `npm run build` 通过。
- 简化详情页浮动置顶栏：用户明确"顶部浮动栏 只有 返回"。移除栏内的 小作文按钮、标题、关闭按钮，仅保留 返回(`navigate(-1)`)。同时清理死代码——删除 `showEssay`/`setShowEssay` 状态、`EssayDetailModal` 渲染行与 import（详情页再无 小作文 入口）；`X` 图标在 475 行仍有他用故保留。已 `npm run build`（tsc -b + vite build）通过，无未使用变量报错。
- 小作文弹窗（及所有 Modal）标题栏浮顶：`Modal.tsx` 的 header 由普通 flex 改为 `sticky top-0 z-10`，并加 `bg-[var(--surface)] border-b border-[var(--line)]` 背景+分隔线（内容滚动时从标题栏下方划过）。
- 【修正·同次】首版用 `-mt-5 sm:-mt-6 + top-0` 负外边距抵消面板 `p-5 sm:p-6` 内边距，实测滚动时标题栏被 `top-0` 钳制在距面板真实上边缘 20px 处（sticky 的 top 相对滚动容器内边距边缘），上方 20px 内边距带无覆盖 → 滚动内容从缝里"穿帮"。**根治**：移除面板自身的 `p-5 sm:p-6` 纵向内边距，让 header `top-0` 直接吸在面板真实顶边；内边距改由内容区 `<div className="p-5 sm:p-6">` 与 footer 各自承载。静止态视觉一致、长内容滚动时标题栏严丝合缝浮顶、不再漏内容。已 `npm run build` 通过。

## 挑战管理页信息架构重做（Option A 三段式分区）
- 用户反馈原挑战管理页「全能卡片」层级混乱：① 展开卡片同时承载信息+管参与者+跑生命周期操作；② 缺"添加参与者"入口（仅能编辑已有）；③ 危险操作（删除/标记失败）与编辑并排；④ 参与者行内编辑 4 控件挤一行；⑤ 编辑表单顶掉整个详情。
- 重做（`AdminPanelPage.tsx`，均 `tsc -b --noEmit` 通过）：
  - 外层容器改 `divide-y` 分隔，**拆成 3 个带标题分区 + 1 个危险操作区**：`挑战信息` / `参与者` / `操作` / `危险操作`（虚线分隔）。
  - **补"添加参与者"入口**：用 store 既有 `joinChallenge(challengeId, participantId, name, stake, side)`（仅 active 且未封档可加）；新增 `canAddParticipant` 判断、`addingToChallenge`+`addParticipantData` 状态、内联添加表单；参与者为空时显示空态+引导。
  - **编辑表单只替换「挑战信息」分区**，参与者/操作仍可见（修问题⑤）。
  - **删除挑战 / 删除参与者** 改用 `useConfirm()` 确认弹窗（对象式 `{title,message,confirmText,cancelText,danger}`），替代裸 `confirm()`；顺带把备份区 3 处裸 `confirm` 也统一为确认弹窗（因组件内 `const { confirm } = useConfirm()` 覆盖了全局 `confirm`）。
  - 新增图标 `UserPlus` 导入与 `useConfirm/useToast` 导入。
- 预览：http://localhost:5173/（HMR 已热更新）→ /admin → 挑战管理，展开任一挑战看分区。

## 管理面板统计卡片：第 3 张「参与者」→「参与次数」
- 用户指管理面板顶部 4 张统计卡（挑战总数 / 进行中 / 参与者 / 总押注）的第 3 张，要改展示"参与次数"。
- 核实：`stats.participants` 本就是所有挑战 `participants.length` 的累加（同人多次参与会重复计数）= 参与总次数，数值逻辑正确，只是标签写成了"参与者"名不副实。
- 改动（`AdminPanelPage.tsx:517`）：仅把 label `'参与者'` → `'参与次数'`，图标/数值不变。纯标签修正，无逻辑改动。HMR 已热更新。

## 参与挑战弹窗优化（仅 P2 + P3）
- 用户就"立即参与"弹窗（`ChallengeDetailPage.tsx` 的 join modal）要优化方案，评估了 P0~P6；最终只选做 **P2（必填标记+禁用原因提示）** 与 **P3（已参与角标）**，P0/P1/P4/P5/P6 暂不动。
- P3（`components/participant/ParticipantGrid.tsx`）：禁用 tile 原本只 `opacity-40` 无说明 → 改为仅内部头像+姓名淡出（`opacity-40` 移到内层 wrapper），并在右上角加 `已参与` 角标（`var(--bad-soft)/var(--bad)`，全透明不受淡出影响），`button` 加 `relative`。
- P2（`ChallengeDetailPage.tsx`）：`选择参与者` 与 `选择你的态度` 标签后加红色 `必填` 小字；弹窗底部（态度区下方、`</Modal>` 前）新增提示——`!canJoin && !joinError` 时显示"还需完成：选择参与者 / 押金 ≥ ¥100 / 选择你的态度"（按缺失项动态拼接），解决确认按钮灰掉却不知差什么的问题。
- `tsc -b --noEmit` 通过（exit 0）。预览重启 http://localhost:5173/（Vite 后台 task Dl2vyt）。

## 参与弹窗·选择参与者模块二次优化（用户追加）
- 用户要求对"选择参与者"模块单独：去掉 `必填` 标记、优化间距、优化模块大小。
- `ChallengeDetailPage.tsx`：删除 `选择参与者` 标签后的 `必填` 红字；容器 `mb-4`→`mb-3`、标签 `mb-3`→`mb-2` 收紧间距（`选择你的态度` 的 `必填` 保留不动，仅改这一个模块）。
- `ParticipantGrid.tsx`（仅此弹窗使用，可安全改）：tile `p-2`→`p-1.5`、`min-h-[80px]`→`min-h-[66px]`、头像 `w-10 h-10`→`w-9 h-9`、网格 `gap-2`→`gap-1.5`、外层 `space-y-3`→`space-y-2`；并给网格包一层 `max-h-[240px] overflow-y-auto scroll-slim pr-1`（人数多/展开后内部滚动，模块高度有上限，弹窗不会过高）。
- `tsc -b --noEmit` 通过（exit 0），预览在线 HMR 热更新。

## 参与弹窗·移除必填标记与底部反馈（用户追加）
- 用户要求：去掉"选择你的态度"后的 `必填`（此前"选择参与者"的已去掉）→ 现在弹窗内两个标签均不再有必填红字。
- 删除底部 `{!canJoin && !joinError && ...}` 的"还需完成：…"提示块（join modal 内不再有任何引导式反馈文字）。
- 注意：`canJoin` 仍用于 footer「确认参与」按钮禁用逻辑、`joinError` 仍用于错误提示框，删除反馈块后无未用变量，`tsc` 通过（exit 0）。

## 参与弹窗·押金模块间距/选项调整（用户追加）
- 移除押金预设按钮中的 `2000`（数组 `[100,200,500,1000,2000]` → `[100,200,500,1000]`），押金模块更紧凑。
- 押金区容器 `mb-6` → `mb-3`（消除按钮下方过大留白）；押金标签 `mb-3` → `mb-2`、态度标签 `mb-3` → `mb-2`，统一为 `mb-2`，修复"按钮上面间距过大"。
- 现状节奏：参与者区 `mb-3` / 押金区 `mb-3` / 态度区 `mb-2`，各标签统一 `mb-2`。`tsc` 通过（exit 0），预览在线。

## 测试环境播种参与者数据
- 需求：本地测试环境（dev server 默认 `envMode: 'test'`，参与者池存 localStorage `participant-data-v1`）初始为空，要加一些示例参与者。
- 做法：`src/store/challengeStore.ts` 新增 `seedParticipants()`（10 个中文名、isActive=true、order 1..10、createdAt/updatedAt 递减），并在 `init()` 的 test 分支里：当 `participant-seed-v1` flag 未设置且 `loadLocalParticipants()` 为空时，自动播种并写入 localStorage、置 flag（保证只播一次，清空后不会反复回灌；已有真实数据则不动）。
- 注意：播种的是「可选参与者池」（`ParticipantItem[]`），不是各挑战的 `Challenge.participants` 参与记录；要看到挑战里有参与者需另行 join。
- `tsc` 通过（exit 0）。生效需刷新页面（init 在 App 挂载时跑）。

## 参与者网格·选中外框被遮挡修复
- 现象：参与弹窗网格中选中参与者的 `ring-2` 外框被遮挡。
- 根因：① 网格容器 `overflow-y-auto` 会裁掉贴边 ring；② 选中 tile 无层级，ring 被相邻 tile 的实色背景覆盖。
- 修复（`components/participant/ParticipantGrid.tsx`）：容器 `pr-1` → `p-2`（ring 有空间、避开滚动条）；选中态 className 加 `z-10`（button 本身已是 `relative`，提升层级盖过邻格）。`tsc` 通过（exit 0），HMR 热更新。

## 创建挑战页·全量重构为双栏+实时预览
- 用户问"创建挑战页有无更好方案"，给出对比草图（单栏无分组 vs 双栏+分组+预览），选定"全量双栏"。
- 重写 `src/pages/CreateChallengePage.tsx`：
  - 布局：`max-w-5xl`，`lg:grid-cols-[minmax(0,1fr)_360px]` 双栏；左=分组表单，右=sticky 实时预览；移动端预览 `order-1` 置顶。
  - 表单分三段 section：基本信息（封面/主题/目标/挑战者）/ 时间（起止日期）/ 押注规则。
  - 实时预览 `PreviewCard` 复用 ChallengeCard 视觉语言（封面16:9+遮罩+预览标、主题、@挑战者、日期、通赔/底仓双卡）。
  - 内联校验：errors 对象 + touched/attempted；缺字段/结束早于开始/底仓<200 红字提示，提交时整体标红并 toast。
  - 金额改 chips+自定义（`MoneyField` 组件，通赔[500,1000,2000,5000]、底仓[200,500,1000]），与参与弹窗一致。
  - 术语统一：最高赔付→通赔、最低赔付→底仓（与详情页一致）。
  - 封面：拖拽区（点击/拖放上传）+ URL 输入 + 内联预览；去掉原 `setTimeout(0)`。
- `tsc` 通过（exit 0），预览在线 HMR 热更新。

## 创建挑战页·术语回退
- 用户要求把创建页内的「通赔/底仓」改回「最高赔付/最低赔付」。
- 改动 `CreateChallengePage.tsx` 内 6 处：表单标签2（最高赔付/最低赔付）、右侧预览卡2（最高赔付/最低赔付）、校验提示2（"最高赔付不能为负数"/"最低赔付不能低于 200"）。`tsc` 通过，已无「通赔/底仓」字样。
- 注意：详情页、统计卡、ChallengeCard 仍用「通赔/底仓」，与创建页术语现不一致；若需全局统一再处理。


